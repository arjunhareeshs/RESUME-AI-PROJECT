pdfplumber==0.11.1      # Main PDF parser, built on pdfminer.six
PyMuPDF==1.24.8         # Used for link extraction (fitz)
python-docx==0.8.11     # For reading .docx files

# Image Handling & OCR
Pillow==10.2.0          # Image processing library (needed by pdfplumber, pdf2image, pytesseract)
pdf2image==1.16.3       # For converting PDF pages to images for OCR fallback
pytesseract==0.3.10     # Tesseract OCR wrapper (fallback OCR)
paddleocr               # Main OCR engine (use latest stable version)
# Note: paddleocr requires paddlepaddle. Install separately: pip install paddlepaddle
opencv-python==4.10.0.84 # Often needed by OCR engines and image preprocessing
scikit-image==0.22.0    # Used by some image processing steps

# Layout Analysis & Utilities
numpy<2.0               # CRITICAL: Version must be < 2.0 for compatibility with paddleocr/faiss
scikit-learn            # For K-Means clustering used in column detection

# Parsing Helpers
fuzzywuzzy              # For fuzzy header matching in parser.py
python-Levenshtein      # Optional: Makes fuzzywuzzy much faster